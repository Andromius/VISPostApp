<UserControl x:Class="UI.Views.PackageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.Views" xmlns:controls="clr-namespace:UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <controls:PackageWithoutCourier Grid.Row="0" DataContext="{Binding NoCourierViewModel}" AddSelectedPackageCommand="{Binding AddSelectedPackageCommand}"/>
    </Grid>
</UserControl>
